spring.application.name=backend

# Active profile: 'dev' for local, 'prod' for Railway deployment
# Switch to prod by setting env var SPRING_PROFILES_ACTIVE=prod on Railway
spring.profiles.active=dev
